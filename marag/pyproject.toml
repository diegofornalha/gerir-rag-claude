[project]
name = "a2a-samples-marag"
version = "0.1.0"
description = "Currency conversion using A2A and marag"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["marag>=3.0.0", "a2a-samples"]

[tool.uv.sources]
a2a-samples = { workspace = true }

[tool.ruff.lint]
extend-select = ["I", "UP"]